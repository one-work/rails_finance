<div class="<%= role_permit?(business: 'finance', namespace: 'me') ? 'box is-padding-less' : 'display-none' %>">
  <div class="box-header">
    <span class="text-grey size-7 has-border-left-primary"><%= t('finance.me.title') %></span>
  </div>
  <div class="fixed-grid has-3-cols is-shadow-top p-3">
    <div class="grid">
      <%= link_to({ controller: 'finance/me/expenses' }, class: 'column has-column-centered') do %>
        <%= svg_tag 'users', class: 'icon is-medium text-primary' %>
        <div class="text-grey size-7 mt-1"><%= t('finance.me.expenses.index.title') %></div>
      <% end %>
    </div>
  </div>
</div>
